// Інтерфейс IFilter описує структуру значень фільтрів для пошуку порід собак
export interface IFilter {
  size: number[]; // Масив значень розміру породи
  energyLvl: number[]; // Масив значень рівня енергії
  easeTr: number[]; // Масив значень легкості навчання
  groomReqs: number[]; // Масив значень вимог до догляду
}